name: Run Multiple Python Scripts

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script:
          - "Skip Tracing/truepeople_tracer1.py"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Set UTF-8 Encoding
        run: echo "PYTHONIOENCODING=UTF-8" >> $GITHUB_ENV

      - name: Install system dependencies for Chrome & Playwright
        run: |
          sudo apt update
          sudo apt install -y \
            libwoff1 libvpx7 libevent-2.1-7 libopus0 \
            libharfbuzz-icu0 libgstallocators-1.0-0 libgstapp-1.0-0 \
            libgstpbutils-1.0-0 libgstaudio-1.0-0 libgsttag-1.0-0 \
            libgstvideo-1.0-0 libgstgl-1.0-0 libgstcodecparsers-1.0-0 \
            libgstfft-1.0-0 libsecret-1-0 libhyphen0 libmanette-0.2-0 \
            libflite1 libflite-dev libflite-usenglish1 \
            libflite-cmu-grapheme-lang1 libflite-cmu-grapheme-lex1 \
            libflite-cmu-indic-lang1 libflite-cmu-indic-lex1 \
            libflite-cmulex1 libflite-cmu-time-awb1 \
            libflite-cmu-us-awb1 libflite-cmu-us-kal16-1 libflite-cmu-us-kal1 \
            libflite-cmu-us-rms1 libflite-cmu-us-slt1 \
            libgles2-mesa libx264-155

      - name: Install Firefox and GeckoDriver (Ubuntu)
        run: |
          sudo apt update
          sudo apt install -y firefox jq wget
          GECKO_VERSION=$(curl -s https://api.github.com/repos/mozilla/geckodriver/releases/latest | jq -r .tag_name | sed 's/v//')
          GECKO_URL="https://github.com/mozilla/geckodriver/releases/download/v$GECKO_VERSION/geckodriver-v$GECKO_VERSION-linux64.tar.gz"
          wget $GECKO_URL -O geckodriver.tar.gz
          tar -xvzf geckodriver.tar.gz
          sudo mv geckodriver /usr/local/bin/
          geckodriver --version

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          pip install setuptools

      - name: Install Playwright browsers
        run: playwright install

      - name: Run Python script
        run: python "${{ matrix.script }}"
